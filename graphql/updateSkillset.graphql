mutation updateSkillset($skillsetId: ID!, $skillsetName: String!, $skillsetDescription: String!) {
  updateSkillset(where: { id: $skillsetId }, data: { name: $skillsetName, description: $skillsetDescription }) {
    id
    name
    description
  }
}
